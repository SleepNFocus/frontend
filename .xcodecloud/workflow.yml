actions:
  - name: Archive
    action: build
    platform: iOS
    scheme: FOCUZ
    workspace: frontend-app/ios/FOCUZ.xcworkspace
    analyze: false
    pre-xcodebuild:
    script: |
      chmod +x $PWD/frontend/ci_scripts/ci_pre_xcodebuild.sh
      $PWD/frontend/ci_scripts/ci_pre_xcodebuild.sh
