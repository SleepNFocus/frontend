actions:
  - name: Archive
    action: build
    platform: iOS
    scheme: FOCUZ
    workspace: frontend-app/ios/FOCUZ.xcworkspace
    analyze: false
    pre-xcodebuild:
      script: |
        chmod +x frontend/ci_scripts/ci_pre_xcodebuild.sh
        ./frontend/ci_scripts/ci_pre_xcodebuild.sh
